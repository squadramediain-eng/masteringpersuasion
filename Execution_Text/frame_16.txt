FRAME_16 — Key Behaviours — Influence Styles
Appears in the video at: 7:49
Scene length: 420 frames = 14.0 seconds (30fps)
Archetype: A · Divider
Beat: Numeral + title, leader vs negative officer.

WORLD — never enters, never exits
  The canvas, the wave band and the rising bubbles are drawn once by
  src/scenes/WorldLayer.tsx and run continuously behind every scene. This
  frame's own #bg and #waves are stripped by motion.ts (SKIP_IDS) so the
  water never doubles or restarts at a cut.

ENTRANCE — contents slide in from the right, decelerating on the brand curve.

ELEMENTS (44, in the order motion.ts wraps them)

  part_1 [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 2.0px / 5.4s.
  part_2 [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 2.0px / 5.4s.
  part_4 [prop]
      enters at 7:51 (471.0s film time = 2.0s into this scene) on the narration beat "section six" (beat 1), typewriter; default idle 2.0px / 5.4s.
  part_5 [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 2.0px / 5.4s.
  part_6 [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 2.0px / 5.4s.
  part_7 [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 2.0px / 5.4s.
  part_8 [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 2.0px / 5.4s.
  part_9 [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 2.0px / 5.4s.
  ch1_x5f_bisep [character]
      enters at 7:56 (476.0s film time = 7.0s into this scene) on the narration beat "key behaviours" (beat 2), slide; default idle 3.0px / 4.6s.
  ch1_x5f_head [character]
      enters at 7:56 (476.0s film time = 7.0s into this scene) on the narration beat "key behaviours" (beat 2), slide; default idle 3.0px / 4.6s.
  ch1_x5f_lip [character]
      enters at 7:56 (476.0s film time = 7.0s into this scene) on the narration beat "key behaviours" (beat 2), slide; default idle 3.0px / 4.6s.
  ch1_x5f_eye1 [character]
      enters at 7:56 (476.0s film time = 7.0s into this scene) on the narration beat "key behaviours" (beat 2), slide; default idle 3.0px / 4.6s.
  ch1_x5f_eye2 [character]
      enters at 7:56 (476.0s film time = 7.0s into this scene) on the narration beat "key behaviours" (beat 2), slide; default idle 3.0px / 4.6s.
  ch1_x5f_eyebrow2 [character]
      enters at 7:56 (476.0s film time = 7.0s into this scene) on the narration beat "key behaviours" (beat 2), slide; default idle 3.0px / 4.6s.
  ch1_x5f_eyebrow [character]
      enters at 7:56 (476.0s film time = 7.0s into this scene) on the narration beat "key behaviours" (beat 2), slide; default idle 3.0px / 4.6s.
  ch1_x5f_nose [character]
      enters at 7:56 (476.0s film time = 7.0s into this scene) on the narration beat "key behaviours" (beat 2), slide; default idle 3.0px / 4.6s.
  ch1_x5f_hand [character]
      enters at 7:56 (476.0s film time = 7.0s into this scene) on the narration beat "key behaviours" (beat 2), slide; default idle 3.0px / 4.6s.
  ch1_x5f_arm [character]
      enters at 7:56 (476.0s film time = 7.0s into this scene) on the narration beat "key behaviours" (beat 2), slide; default idle 3.0px / 4.6s.
  part_10 [prop]
      enters at 7:56 (476.0s film time = 7.0s into this scene) on the narration beat "key behaviours" (beat 2), fade; default idle 2.0px / 5.4s.
  part_11 [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 2.0px / 5.4s.
  ch2_x5f_bisep2 [character]
      enters at 7:56 (476.0s film time = 7.0s into this scene) on the narration beat "key behaviours" (beat 2), slide; default idle 3.0px / 4.6s.
  ch2_x5f_palm1 [character]
      enters at 7:56 (476.0s film time = 7.0s into this scene) on the narration beat "key behaviours" (beat 2), slide; default idle 3.0px / 4.6s.
  ch2_x5f_palm2 [character]
      enters at 7:56 (476.0s film time = 7.0s into this scene) on the narration beat "key behaviours" (beat 2), slide; default idle 3.0px / 4.6s.
  ch2_x5f_bisep1 [character]
      enters at 7:56 (476.0s film time = 7.0s into this scene) on the narration beat "key behaviours" (beat 2), slide; default idle 3.0px / 4.6s.
  ch2_x5f_arm2 [character]
      enters at 7:56 (476.0s film time = 7.0s into this scene) on the narration beat "key behaviours" (beat 2), slide; default idle 3.0px / 4.6s.
  ch2_x5f_arm1 [character]
      enters at 7:56 (476.0s film time = 7.0s into this scene) on the narration beat "key behaviours" (beat 2), slide; default idle 3.0px / 4.6s.
  ch1_x5f_head_2 [character]
      enters at 7:56 (476.0s film time = 7.0s into this scene) on the narration beat "key behaviours" (beat 2), slide; default idle 3.0px / 4.6s.
  ch2_x5f_eyebrow2 [character]
      enters at 7:56 (476.0s film time = 7.0s into this scene) on the narration beat "key behaviours" (beat 2), slide; default idle 3.0px / 4.6s.
  ch2_x5f_eyebrow1 [character]
      enters at 7:56 (476.0s film time = 7.0s into this scene) on the narration beat "key behaviours" (beat 2), slide; default idle 3.0px / 4.6s.
  ch2_x5f_eye2 [character]
      enters at 7:56 (476.0s film time = 7.0s into this scene) on the narration beat "key behaviours" (beat 2), slide; default idle 3.0px / 4.6s.
  ch2_x5f_eye1 [character]
      enters at 7:56 (476.0s film time = 7.0s into this scene) on the narration beat "key behaviours" (beat 2), slide; default idle 3.0px / 4.6s.
  ch2_x5f_lip [character]
      enters at 7:56 (476.0s film time = 7.0s into this scene) on the narration beat "key behaviours" (beat 2), slide; default idle 3.0px / 4.6s.
  ch2_x5f_nose [character]
      enters at 7:56 (476.0s film time = 7.0s into this scene) on the narration beat "key behaviours" (beat 2), slide; default idle 3.0px / 4.6s.
  part_12 [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 2.0px / 5.4s.
  part_13 [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 2.0px / 5.4s.
  part_14 [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 2.0px / 5.4s.
  part_15 [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 2.0px / 5.4s.
  part_16 [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 2.0px / 5.4s.
  part_17 [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 2.0px / 5.4s.
  part_18 [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 2.0px / 5.4s.
  text_0 "Key Behaviours"
      enters 0.42s (guider spec, rise-fade); types on over 1.00s (14 chars @ 14/s, 1 line) with a caret.
  text_1 "Influence Styles"
      enters 0.55s (guider spec, rise-fade); types on over 1.14s (16 chars @ 14/s, 1 line) with a caret.
  text_2 "6"
      enters 0.68s (guider spec, rise-fade); types on over 0.07s (1 chars @ 14/s, 1 line) with a caret.
  text_3 "LEADER"
      enters on the fallback recipe (no spec entry — role-based stagger); types on over 0.43s (6 chars @ 14/s, 1 line) with a caret.

HOLD — once everything has landed, the idle loops, orbit rings, wave drift and
  bubbles keep running. The frame is never completely still.

EXIT — contents slide out to the left over the last 30 frames and fade
  over the back half of that travel, so the frame is clear before the next
  subject arrives. The world does NOT fade — that is what stops a transition
  going blank.

## Notes
- Generated by scripts/build-execution-text.js from sceneRegistry.ts, the frame
  SVG, animation.json, audio-cues.json, text-metrics.json and
  frame-overrides.json. Re-run it after any frame re-export rather than
  hand-editing, or this record drifts from what actually renders.
- To change any element here, edit public/animation/frame-overrides.json
  (see CLAUDE.md "Tweak Any Frame"). Run scripts/inspect-frame.js to see which
  id is which on screen.
- NARRATION TIMING: 25 element(s) in this frame carry data-t authored into
  the artwork by the design project. That timing travels WITH the element, so a
  future rename cannot orphan it the way the mp_v2 redesign orphaned all 133
  entries in audio-cues.json (those are preserved in audio-cues.pre-mp_v2.json).
  To retime one, prefer data-t upstream; frame-overrides.json "at" wins locally.
