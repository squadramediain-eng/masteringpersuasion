FRAME_04 — Persuasion Steps
Appears in the video at: 1:52
Scene length: 1080 frames = 36.0 seconds (30fps)
Archetype: F · Step flow
Beat: Three steps pop left-to-right, arrows draw between them.

WORLD — never enters, never exits
  The canvas, the wave band and the rising bubbles are drawn once by
  src/scenes/WorldLayer.tsx and run continuously behind every scene. This
  frame's own #bg and #waves are stripped by motion.ts (SKIP_IDS) so the
  water never doubles or restarts at a cut.

ENTRANCE — contents slide in from the right, decelerating on the brand curve.

ELEMENTS (29, in the order motion.ts wraps them)

  part_1 [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 4.0px / 5.0s.
  part_2 [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 4.0px / 5.0s.
  bubbles [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 4.0px / 5.0s.
  line [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 4.0px / 5.0s.
  h1 [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 4.0px / 5.0s.
  scenario_disc_1 [circle]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 4.0px / 5.2s.
  c1 [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 4.0px / 5.0s.
  scenario_disc_2 [circle]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 4.0px / 5.2s.
  c1_2 [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 4.0px / 5.0s.
  step_1_disc [circle]
      enters at 1:56 (116.0s film time = 4.0s into this scene) on the narration beat "cite the facts" (beat 1), pop; default idle 4.0px / 5.2s.
  c1_3 [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 4.0px / 5.0s.
  step_2_disc [circle]
      enters at 2:02 (122.0s film time = 10.0s into this scene) on the narration beat "state the risk" (beat 2), pop; default idle 4.0px / 5.2s.
  c1_4 [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 4.0px / 5.0s.
  part_8 [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 4.0px / 5.0s.
  scenario_disc_3 [circle]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 4.0px / 5.2s.
  c1_5 [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 4.0px / 5.0s.
  step_3_disc [circle]
      enters at 2:10 (130.0s film time = 18.0s into this scene) on the narration beat "clear solution" (beat 3), pop; default idle 4.0px / 5.2s.
  c1_6 [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 4.0px / 5.0s.
  connectors [arrow]
      enters on the fallback recipe (no spec entry — role-based stagger).
  step_labels [prop]
      enters at 1:56 (116.0s film time = 4.0s into this scene) on the narration beat "cite the facts" (beat 1), pop; default idle 4.0px / 5.0s.
  title [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 4.0px / 5.0s.
  env_backdrop [background]
      enters on the fallback recipe (no spec entry — role-based stagger).
  text_0 "Clear Facts"
      enters 0.42s (guider spec, rise-fade); types on over 0.79s (11 chars @ 14/s, 1 line) with a caret.
  text_1 "Cite the procedure directly"
      enters on the fallback recipe (no spec entry — role-based stagger); types on over 1.93s (27 chars @ 14/s, 1 line) with a caret.
  text_2 "State Risk"
      enters on the fallback recipe (no spec entry — role-based stagger); types on over 0.71s (10 chars @ 14/s, 1 line) with a caret.
  text_3 "Identify the dangers calmly."
      enters on the fallback recipe (no spec entry — role-based stagger); types on over 2.00s (28 chars @ 14/s, 1 line) with a caret.
  text_4 "Offer Solution"
      enters on the fallback recipe (no spec entry — role-based stagger); types on over 1.00s (14 chars @ 14/s, 1 line) with a caret.
  text_5 "Propose a clear alternative."
      enters on the fallback recipe (no spec entry — role-based stagger); types on over 2.00s (28 chars @ 14/s, 1 line) with a caret.
  text_6 "Persuasion Steps"
      enters on the fallback recipe (no spec entry — role-based stagger); types on over 1.14s (16 chars @ 14/s, 1 line) with a caret.

HOLD — once everything has landed, the idle loops, orbit rings, wave drift and
  bubbles keep running. The frame is never completely still.

EXIT — contents slide out to the left over the last 30 frames and fade
  over the back half of that travel, so the frame is clear before the next
  subject arrives. The world does NOT fade — that is what stops a transition
  going blank.

## Notes
- Generated by scripts/build-execution-text.js from sceneRegistry.ts, the frame
  SVG, animation.json, audio-cues.json, text-metrics.json and
  frame-overrides.json. Re-run it after any frame re-export rather than
  hand-editing, or this record drifts from what actually renders.
- To change any element here, edit public/animation/frame-overrides.json
  (see CLAUDE.md "Tweak Any Frame"). Run scripts/inspect-frame.js to see which
  id is which on screen.
- NARRATION TIMING: 4 element(s) in this frame carry data-t authored into
  the artwork by the design project. That timing travels WITH the element, so a
  future rename cannot orphan it the way the mp_v2 redesign orphaned all 133
  entries in audio-cues.json (those are preserved in audio-cues.pre-mp_v2.json).
  To retime one, prefer data-t upstream; frame-overrides.json "at" wins locally.
