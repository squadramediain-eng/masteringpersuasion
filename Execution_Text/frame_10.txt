FRAME_10 — The 30-Second Metric
Appears in the video at: 4:46
Scene length: 870 frames = 29.0 seconds (30fps)
Archetype: D · Stat
Beat: "30" counts up and pops; icons orbit in quietly.

WORLD — never enters, never exits
  The canvas, the wave band and the rising bubbles are drawn once by
  src/scenes/WorldLayer.tsx and run continuously behind every scene. This
  frame's own #bg and #waves are stripped by motion.ts (SKIP_IDS) so the
  water never doubles or restarts at a cut.

ENTRANCE — contents slide in from the right, decelerating on the brand curve.

ELEMENTS (25, in the order motion.ts wraps them)

  sb_1 [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 4.0px / 5.0s.
  env_stone [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 4.0px / 5.0s.
  bubbles [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 4.0px / 5.0s.
  env_coral_1 [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 4.0px / 5.0s.
  env_coral_2 [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 4.0px / 5.0s.
  env_fish [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 4.0px / 5.0s.
  stat [stat]
      enters at 4:49 (289.0s film time = 3.0s into this scene) on the narration beat "specific metric" (beat 1), count; default idle 3.4px / 5.4s.
  orbit_disc_1 [circle]
      enters 305.00s (frame-overrides); default idle 4.0px / 5.2s.
  orbit_disc_2 [circle]
      enters 306.20s (frame-overrides); default idle 4.0px / 5.2s.
  orbit_disc_3 [circle]
      enters 307.40s (frame-overrides); default idle 4.0px / 5.2s.
  orbit_disc_4 [circle]
      enters 308.60s (frame-overrides); default idle 4.0px / 5.2s.
  icon_family [icon]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 4.5px / 4.6s.
  icon_chart [icon]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 4.5px / 4.6s.
  icon_mountain [icon]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 4.5px / 4.6s.
  icon_clock_2 [icon]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 4.5px / 4.6s.
  f10_clockR [icon]
      enters on the fallback recipe (no spec entry — role-based stagger); orbit 360deg / 6.0s (override); default idle 4.5px / 4.6s.
  icon_shield [icon]
      enters 1.01s (guider spec, pop); default idle 4.5px / 4.6s.
  f10_arrow2 [arrow]
      enters on the fallback recipe (no spec entry — role-based stagger); orbit -360deg / 8.0s (override).
  f10_shield [icon]
      enters on the fallback recipe (no spec entry — role-based stagger); idle 0px / 1.6s (override).
  f10_arrow1 [arrow]
      enters on the fallback recipe (no spec entry — role-based stagger); orbit -360deg / 8.0s (override).
  icon_person [character]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 5.0px / 4.2s.
  icon_clock_1 [icon]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 4.5px / 4.6s.
  f10_clockL [icon]
      enters on the fallback recipe (no spec entry — role-based stagger); orbit 360deg / 6.0s (override); default idle 4.5px / 4.6s.
  guide_grid [decor]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 3.0px / 6.6s.
  text_0 "30"
      enters 0.42s (guider spec, rise-fade); types on over 0.14s (2 chars @ 14/s, 1 line) with a caret.

HOLD — once everything has landed, the idle loops, orbit rings, wave drift and
  bubbles keep running. The frame is never completely still.

EXIT — contents slide out to the left over the last 30 frames and fade
  over the back half of that travel, so the frame is clear before the next
  subject arrives. The world does NOT fade — that is what stops a transition
  going blank.

## Notes
- Generated by scripts/build-execution-text.js from sceneRegistry.ts, the frame
  SVG, animation.json, audio-cues.json, text-metrics.json and
  frame-overrides.json. Re-run it after any frame re-export rather than
  hand-editing, or this record drifts from what actually renders.
- To change any element here, edit public/animation/frame-overrides.json
  (see CLAUDE.md "Tweak Any Frame"). Run scripts/inspect-frame.js to see which
  id is which on screen.
- NARRATION TIMING: 5 element(s) in this frame carry data-t authored into
  the artwork by the design project. That timing travels WITH the element, so a
  future rename cannot orphan it the way the mp_v2 redesign orphaned all 133
  entries in audio-cues.json (those are preserved in audio-cues.pre-mp_v2.json).
  To retime one, prefer data-t upstream; frame-overrides.json "at" wins locally.
