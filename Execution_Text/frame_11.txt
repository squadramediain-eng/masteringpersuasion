FRAME_11 — Resolving Disputes — Terminal Conflicts
Appears in the video at: 5:15
Scene length: 600 frames = 20.0 seconds (30fps)
Archetype: A · Divider
Beat: Numeral + title, container ship + officer enter.

WORLD — never enters, never exits
  The canvas, the wave band and the rising bubbles are drawn once by
  src/scenes/WorldLayer.tsx and run continuously behind every scene. This
  frame's own #bg and #waves are stripped by motion.ts (SKIP_IDS) so the
  water never doubles or restarts at a cut.

ENTRANCE — contents slide in from the right, decelerating on the brand curve.

ELEMENTS (17, in the order motion.ts wraps them)

  ref [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 2.0px / 5.4s.
  ship_x5f_1 [ship]
      enters at 5:21 (321.0s film time = 6.0s into this scene) on the narration beat "terminal conflicts" (beat 2), slide; default idle 4.5px / 5.2s.
  x36_1 [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 2.0px / 5.4s.
  x31_7 [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 2.0px / 5.4s.
  x31_8 [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 2.0px / 5.4s.
  icon_x5f_2 [icon]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 2.2px / 5.0s.
  icon_x5f_1 [icon]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 2.2px / 5.0s.
  person_x5f_2 [character]
      enters at 5:21 (321.0s film time = 6.0s into this scene) on the narration beat "terminal conflicts" (beat 2), slide; default idle 3.0px / 4.6s.
  person_x5f_1 [character]
      enters at 5:21 (321.0s film time = 6.0s into this scene) on the narration beat "terminal conflicts" (beat 2), slide; default idle 3.0px / 4.6s.
  bubble [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 2.0px / 5.4s.
  number [numeral]
      enters at 5:17 (317.0s film time = 2.0s into this scene) on the narration beat "section four" (beat 1), typewriter; default idle 1.8px / 6.6s.
  sub_x5f_text [prop]
      enters at 5:17 (317.0s film time = 2.0s into this scene) on the narration beat "section four" (beat 1), typewriter; default idle 2.0px / 5.4s.
  main_x5f_text [prop]
      enters at 5:17 (317.0s film time = 2.0s into this scene) on the narration beat "section four" (beat 1), typewriter; default idle 2.0px / 5.4s.
  guide [decor]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 1.4px / 7.2s.
  text_0 "4"
      enters 0.42s (guider spec, rise-fade); types on over 0.07s (1 chars @ 14/s, 1 line) with a caret.
  text_1 "Terminal Conflicts"
      enters 0.55s (guider spec, rise-fade); types on over 1.29s (18 chars @ 14/s, 1 line) with a caret.
  text_2 "Resolving Disputes"
      enters 0.68s (guider spec, rise-fade); types on over 1.29s (18 chars @ 14/s, 1 line) with a caret.

HOLD — once everything has landed, the idle loops, orbit rings, wave drift and
  bubbles keep running. The frame is never completely still.

EXIT — contents slide out to the left over the last 30 frames and fade
  over the back half of that travel, so the frame is clear before the next
  subject arrives. The world does NOT fade — that is what stops a transition
  going blank.

## Notes
- Generated by scripts/build-execution-text.js from sceneRegistry.ts, the frame
  SVG, animation.json, audio-cues.json, text-metrics.json and
  frame-overrides.json. Re-run it after any frame re-export rather than
  hand-editing, or this record drifts from what actually renders.
- To change any element here, edit public/animation/frame-overrides.json
  (see CLAUDE.md "Tweak Any Frame"). Run scripts/inspect-frame.js to see which
  id is which on screen.
- NARRATION TIMING: 6 element(s) in this frame carry data-t authored into
  the artwork by the design project. That timing travels WITH the element, so a
  future rename cannot orphan it the way the mp_v2 redesign orphaned all 133
  entries in audio-cues.json (those are preserved in audio-cues.pre-mp_v2.json).
  To retime one, prefer data-t upstream; frame-overrides.json "at" wins locally.
