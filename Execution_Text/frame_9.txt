FRAME_09 — Strategy Table
Appears in the video at: 4:12
Scene length: 1020 frames = 34.0 seconds (30fps)
Archetype: C · Table
Beat: Table builds row by row; results colour in.

WORLD — never enters, never exits
  The canvas, the wave band and the rising bubbles are drawn once by
  src/scenes/WorldLayer.tsx and run continuously behind every scene. This
  frame's own #bg and #waves are stripped by motion.ts (SKIP_IDS) so the
  water never doubles or restarts at a cut.

ENTRANCE — contents slide in from the right, decelerating on the brand curve.

ELEMENTS (30, in the order motion.ts wraps them)

  env_hand [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 4.0px / 5.0s.
  env_anchor [icon]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 4.5px / 4.6s.
  env_lifering [icon]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 4.5px / 4.6s.
  env_compass [icon]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 4.5px / 4.6s.
  ship [ship]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 9.0px / 4.8s.
  table_body [card]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 2.6px / 6.0s.
  result_icon_passive [icon]
      enters at 4:36 (276.0s film time = 24.0s into this scene) on the narration beat "passive" (beat 4), row; default idle 4.5px / 4.6s.
  result_icon_rational [icon]
      enters at 4:28 (268.0s film time = 16.0s into this scene) on the narration beat "rational buy-in" (beat 3), row; default idle 4.5px / 4.6s.
  icon4_2_2 [icon]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 4.5px / 4.6s.
  result_icon_aggressive [icon]
      enters at 4:20 (260.0s film time = 8.0s into this scene) on the narration beat "aggressive push" (beat 2), row; default idle 4.5px / 4.6s.
  result_icon_extra [icon]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 4.5px / 4.6s.
  row_divider [numeral]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 3.0px / 6.0s.
  row_labels [card]
      enters at 4:20 (260.0s film time = 8.0s into this scene) on the narration beat "aggressive push" (beat 2), row; default idle 2.6px / 6.0s.
  header_bar [prop]
      enters at 4:15 (255.0s film time = 3.0s into this scene) on the narration beat "strategy" (beat 1), header; default idle 4.0px / 5.0s.
  header_labels [prop]
      enters at 4:15 (255.0s film time = 3.0s into this scene) on the narration beat "strategy" (beat 1), header; default idle 4.0px / 5.0s.
  text_0 "N"
      enters on the fallback recipe (no spec entry — role-based stagger); types on over 0.07s (1 chars @ 14/s, 1 line) with a caret.
  text_1 "S"
      enters on the fallback recipe (no spec entry — role-based stagger); types on over 0.07s (1 chars @ 14/s, 1 line) with a caret.
  text_2 "W"
      enters on the fallback recipe (no spec entry — role-based stagger); types on over 0.07s (1 chars @ 14/s, 1 line) with a caret.
  text_3 "E"
      enters on the fallback recipe (no spec entry — role-based stagger); types on over 0.07s (1 chars @ 14/s, 1 line) with a caret.
  text_4 "Aggressive"
      enters on the fallback recipe (no spec entry — role-based stagger); types on over 0.71s (10 chars @ 14/s, 1 line) with a caret.
  text_5 "Rational"
      enters on the fallback recipe (no spec entry — role-based stagger); types on over 0.57s (8 chars @ 14/s, 1 line) with a caret.
  text_6 "Use threats"
      enters on the fallback recipe (no spec entry — role-based stagger); types on over 0.79s (11 chars @ 14/s, 1 line) with a caret.
  text_7 "Resentment"
      enters on the fallback recipe (no spec entry — role-based stagger); types on over 0.71s (10 chars @ 14/s, 1 line) with a caret.
  text_8 "Use safety facts"
      enters on the fallback recipe (no spec entry — role-based stagger); types on over 1.14s (16 chars @ 14/s, 1 line) with a caret.
  text_9 "Persuasion"
      enters on the fallback recipe (no spec entry — role-based stagger); types on over 0.71s (10 chars @ 14/s, 1 line) with a caret.
  text_10 "Ignore issues"
      enters on the fallback recipe (no spec entry — role-based stagger); types on over 0.93s (13 chars @ 14/s, 1 line) with a caret.
  text_11 "Danger"
      enters on the fallback recipe (no spec entry — role-based stagger); types on over 0.43s (6 chars @ 14/s, 1 line) with a caret.
  text_12 "Strategy"
      enters on the fallback recipe (no spec entry — role-based stagger); types on over 0.57s (8 chars @ 14/s, 1 line) with a caret.
  text_13 "Approach Example"
      enters on the fallback recipe (no spec entry — role-based stagger); types on over 1.14s (16 chars @ 14/s, 1 line) with a caret.
  text_14 "Result"
      enters on the fallback recipe (no spec entry — role-based stagger); types on over 0.43s (6 chars @ 14/s, 1 line) with a caret.

HOLD — once everything has landed, the idle loops, orbit rings, wave drift and
  bubbles keep running. The frame is never completely still.

EXIT — contents slide out to the left over the last 30 frames and fade
  over the back half of that travel, so the frame is clear before the next
  subject arrives. The world does NOT fade — that is what stops a transition
  going blank.

## Notes
- Generated by scripts/build-execution-text.js from sceneRegistry.ts, the frame
  SVG, animation.json, audio-cues.json, text-metrics.json and
  frame-overrides.json. Re-run it after any frame re-export rather than
  hand-editing, or this record drifts from what actually renders.
- To change any element here, edit public/animation/frame-overrides.json
  (see CLAUDE.md "Tweak Any Frame"). Run scripts/inspect-frame.js to see which
  id is which on screen.
- NARRATION TIMING: 6 element(s) in this frame carry data-t authored into
  the artwork by the design project. That timing travels WITH the element, so a
  future rename cannot orphan it the way the mp_v2 redesign orphaned all 133
  entries in audio-cues.json (those are preserved in audio-cues.pre-mp_v2.json).
  To retime one, prefer data-t upstream; frame-overrides.json "at" wins locally.
