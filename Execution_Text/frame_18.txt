FRAME_18 — Persuasive Leader Traits
Appears in the video at: 8:32
Scene length: 1050 frames = 35.0 seconds (30fps)
Archetype: G · Circle cluster
Beat: Hero master sets centre; four trait circles pop around.

WORLD — never enters, never exits
  The canvas, the wave band and the rising bubbles are drawn once by
  src/scenes/WorldLayer.tsx and run continuously behind every scene. This
  frame's own #bg and #waves are stripped by motion.ts (SKIP_IDS) so the
  water never doubles or restarts at a cut.

ENTRANCE — contents slide in from the right, decelerating on the brand curve.

ELEMENTS (34, in the order motion.ts wraps them)

  sb_1 [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 4.0px / 5.0s.
  deco_backdrop [background]
      enters on the fallback recipe (no spec entry — role-based stagger).
  trait_disc_3 [circle]
      enters at 8:46 (526.0s film time = 14.0s into this scene) on the narration beat "actively listen" (beat 3), pop; default idle 4.0px / 5.2s.
  f18_puzzle [icon]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 4.5px / 4.6s.
  trait_disc_2 [circle]
      enters at 8:52 (532.0s film time = 20.0s into this scene) on the narration beat "common ground" (beat 4), pop; default idle 4.0px / 5.2s.
  trait_disc_1 [circle]
      enters at 8:40 (520.0s film time = 8.0s into this scene) on the narration beat "calm and rational" (beat 2), pop; default idle 4.0px / 5.2s.
  f18_scales [icon]
      enters on the fallback recipe (no spec entry — role-based stagger); idle 0px / 2.6s (override).
  trait_disc_4 [circle]
      enters at 8:58 (538.0s film time = 26.0s into this scene) on the narration beat "buy-in" (beat 5), pop; default idle 4.0px / 5.2s.
  glyph_common [prop]
      enters at 8:52 (532.0s film time = 20.0s into this scene) on the narration beat "common ground" (beat 4), pop; default idle 4.0px / 5.0s.
  f18_ear [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 4.0px / 5.0s.
  glyph_buyin [prop]
      enters at 8:58 (538.0s film time = 26.0s into this scene) on the narration beat "buy-in" (beat 5), pop; default idle 4.0px / 5.0s.
  glyph_calm [prop]
      enters at 8:40 (520.0s film time = 8.0s into this scene) on the narration beat "calm and rational" (beat 2), pop; default idle 4.0px / 5.0s.
  glyph_listen [prop]
      enters at 8:46 (526.0s film time = 14.0s into this scene) on the narration beat "actively listen" (beat 3), pop; default idle 4.0px / 5.0s.
  leader [character]
      enters at 8:35 (515.0s film time = 3.0s into this scene) on the narration beat "persuasive leader" (beat 1), slide; default idle 5.0px / 4.2s.
  leader_base [character]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 5.0px / 4.2s.
  bisep2 [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 4.0px / 5.0s.
  bisep1 [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 4.0px / 5.0s.
  arm2 [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 4.0px / 5.0s.
  arm1 [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 4.0px / 5.0s.
  leader_shadow [character]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 5.0px / 4.2s.
  leg2 [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 4.0px / 5.0s.
  leg1 [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 4.0px / 5.0s.
  torso [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 4.0px / 5.0s.
  head [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 4.0px / 5.0s.
  eyebrow1 [card]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 2.6px / 6.0s.
  eyebrow2 [card]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 2.6px / 6.0s.
  eye2 [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 4.0px / 5.0s.
  eye1 [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 4.0px / 5.0s.
  lip [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 4.0px / 5.0s.
  nose [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 4.0px / 5.0s.
  title [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 4.0px / 5.0s.
  guide_grid [decor]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 3.0px / 6.6s.
  part_9 [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 4.0px / 5.0s.
  text_0 "Persuasive Leader Traits"
      enters 0.42s (guider spec, rise-fade); types on over 1.71s (24 chars @ 14/s, 1 line) with a caret.

HOLD — once everything has landed, the idle loops, orbit rings, wave drift and
  bubbles keep running. The frame is never completely still.

EXIT — contents slide out to the left over the last 30 frames and fade
  over the back half of that travel, so the frame is clear before the next
  subject arrives. The world does NOT fade — that is what stops a transition
  going blank.

## Notes
- Generated by scripts/build-execution-text.js from sceneRegistry.ts, the frame
  SVG, animation.json, audio-cues.json, text-metrics.json and
  frame-overrides.json. Re-run it after any frame re-export rather than
  hand-editing, or this record drifts from what actually renders.
- To change any element here, edit public/animation/frame-overrides.json
  (see CLAUDE.md "Tweak Any Frame"). Run scripts/inspect-frame.js to see which
  id is which on screen.
- NARRATION TIMING: 9 element(s) in this frame carry data-t authored into
  the artwork by the design project. That timing travels WITH the element, so a
  future rename cannot orphan it the way the mp_v2 redesign orphaned all 133
  entries in audio-cues.json (those are preserved in audio-cues.pre-mp_v2.json).
  To retime one, prefer data-t upstream; frame-overrides.json "at" wins locally.
