FRAME_03 — Section 1 — Safety Tool · AB & Bosun
Appears in the video at: 1:29
Scene length: 690 frames = 23.0 seconds (30fps)
Archetype: A · Divider
Beat: Numeral scales up, AB & bosun scene enters, hazard cue pops.

WORLD — never enters, never exits
  The canvas, the wave band and the rising bubbles are drawn once by
  src/scenes/WorldLayer.tsx and run continuously behind every scene. This
  frame's own #bg and #waves are stripped by motion.ts (SKIP_IDS) so the
  water never doubles or restarts at a cut.

ENTRANCE — contents slide in from the right, decelerating on the brand curve.

ELEMENTS (43, in the order motion.ts wraps them)

  bg_detail_1 [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 4.0px / 5.0s.
  bg_detail_2 [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 4.0px / 5.0s.
  bg_detail_3 [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 4.0px / 5.0s.
  bg_detail_4 [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 4.0px / 5.0s.
  helmet_disc [circle]
      enters 93.00s (frame-overrides); idle 4px / 4.6s (override).
  mariner_extra [character]
      enters 93.20s (frame-overrides); idle 4.5px / 4.0s (override).
  mariner_officer [character]
      enters 92.00s (frame-overrides); idle 4.5px / 4.4s (override).
  section_title [prop]
      enters at 1:31 (91.0s film time = 2.0s into this scene) on the narration beat "section one" (beat 1), typewriter; default idle 4.0px / 5.0s.
  first [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 4.0px / 5.0s.
  heading [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 4.0px / 5.0s.
  heading_2 [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 4.0px / 5.0s.
  mariner_2 [character]
      enters 92.60s (frame-overrides); idle 4.5px / 4.8s (override).
  f3_lifering [icon]
      enters on the fallback recipe (no spec entry — role-based stagger); orbit 360deg / 8.0s (override); default idle 4.5px / 4.6s.
  ab_bosun_pair [character]
      enters 94.50s (frame-overrides); idle 5px / 5.0s (override).
  f3_spanner [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); idle 0px / 1.5s (override).
  f3_gear1 [icon]
      enters on the fallback recipe (no spec entry — role-based stagger); orbit 360deg / 5.0s (override); default idle 4.5px / 4.6s.
  f3_gear2 [icon]
      enters on the fallback recipe (no spec entry — role-based stagger); orbit -360deg / 4.5s (override); default idle 4.5px / 4.6s.
  bg_detail_5 [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 4.0px / 5.0s.
  bg_detail_6 [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 4.0px / 5.0s.
  bg_detail_7 [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 4.0px / 5.0s.
  bg_detail_8 [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 4.0px / 5.0s.
  bg_detail_9 [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 4.0px / 5.0s.
  bg_detail_10 [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 4.0px / 5.0s.
  bg_detail_11 [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 4.0px / 5.0s.
  bg_detail_12 [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 4.0px / 5.0s.
  bg_detail_13 [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 4.0px / 5.0s.
  bg_detail_14 [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 4.0px / 5.0s.
  bg_detail_15 [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 4.0px / 5.0s.
  bg_detail_16 [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 4.0px / 5.0s.
  bg_detail_17 [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 4.0px / 5.0s.
  bg_detail_18 [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 4.0px / 5.0s.
  bg_detail_19 [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 4.0px / 5.0s.
  bg_detail_20 [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 4.0px / 5.0s.
  bg_detail_21 [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 4.0px / 5.0s.
  bg_detail_22 [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 4.0px / 5.0s.
  bg_detail_23 [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 4.0px / 5.0s.
  bg_detail_24 [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 4.0px / 5.0s.
  bg_detail_25 [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 4.0px / 5.0s.
  bg_detail_26 [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 4.0px / 5.0s.
  bg_detail_27 [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 4.0px / 5.0s.
  text_0 "AB &amp; Bosun Scenario"
      enters 0.42s (guider spec, rise-fade); types on over 1.36s (19 chars @ 14/s, 1 line) with a caret.
  text_1 "1"
      enters 0.55s (guider spec, rise-fade); types on over 0.07s (1 chars @ 14/s, 1 line) with a caret.
  text_2 "Safety Tool"
      enters 0.68s (guider spec, rise-fade); types on over 0.79s (11 chars @ 14/s, 1 line) with a caret.

HOLD — once everything has landed, the idle loops, orbit rings, wave drift and
  bubbles keep running. The frame is never completely still.

EXIT — contents slide out to the left over the last 30 frames and fade
  over the back half of that travel, so the frame is clear before the next
  subject arrives. The world does NOT fade — that is what stops a transition
  going blank.

## Notes
- Generated by scripts/build-execution-text.js from sceneRegistry.ts, the frame
  SVG, animation.json, audio-cues.json, text-metrics.json and
  frame-overrides.json. Re-run it after any frame re-export rather than
  hand-editing, or this record drifts from what actually renders.
- To change any element here, edit public/animation/frame-overrides.json
  (see CLAUDE.md "Tweak Any Frame"). Run scripts/inspect-frame.js to see which
  id is which on screen.
- NARRATION TIMING: 6 element(s) in this frame carry data-t authored into
  the artwork by the design project. That timing travels WITH the element, so a
  future rename cannot orphan it the way the mp_v2 redesign orphaned all 133
  entries in audio-cues.json (those are preserved in audio-cues.pre-mp_v2.json).
  To retime one, prefer data-t upstream; frame-overrides.json "at" wins locally.
