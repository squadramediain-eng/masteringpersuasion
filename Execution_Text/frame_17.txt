FRAME_17 — Behaviour & Influence Table
Appears in the video at: 8:03
Scene length: 870 frames = 29.0 seconds (30fps)
Archetype: C · Table
Beat: Behaviour rows build; positive cyan, negative alert.

WORLD — never enters, never exits
  The canvas, the wave band and the rising bubbles are drawn once by
  src/scenes/WorldLayer.tsx and run continuously behind every scene. This
  frame's own #bg and #waves are stripped by motion.ts (SKIP_IDS) so the
  water never doubles or restarts at a cut.

ENTRANCE — contents slide in from the right, decelerating on the brand curve.

ELEMENTS (85, in the order motion.ts wraps them)

  bg_canvas [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 2.0px / 5.4s.
  deco_1 [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 2.0px / 5.4s.
  table_body [card]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 1.6px / 6.4s.
  digit_2 [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 2.0px / 5.4s.
  row_actions [card]
      enters at 8:20 (500.0s film time = 17.0s into this scene) on the narration beat "aggressively shouting" (beat 3), row; default idle 1.6px / 6.4s.
  nmbr [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 2.0px / 5.4s.
  nmbr_2 [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 2.0px / 5.4s.
  row_values [card]
      enters at 8:12 (492.0s film time = 9.0s into this scene) on the narration beat "calmly propose" (beat 2), row; default idle 1.6px / 6.4s.
  digit_2_2 [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 2.0px / 5.4s.
  digit_2_3 [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 2.0px / 5.4s.
  digit_2_4 [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 2.0px / 5.4s.
  digit_2_5 [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 2.0px / 5.4s.
  header_action [prop]
      enters at 8:06 (486.0s film time = 3.0s into this scene) on the narration beat "influence styles" (beat 1), header; default idle 2.0px / 5.4s.
  location [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 2.0px / 5.4s.
  header_labels [prop]
      enters at 8:06 (486.0s film time = 3.0s into this scene) on the narration beat "influence styles" (beat 1), header; default idle 2.0px / 5.4s.
  observation [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 2.0px / 5.4s.
  observation_2 [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 2.0px / 5.4s.
  part_7 [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 2.0px / 5.4s.
  negative_1 [prop]
      enters at 8:20 (500.0s film time = 17.0s into this scene) on the narration beat "aggressively shouting" (beat 3), row; default idle 2.0px / 5.4s.
  ch4_bg [character]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 3.0px / 4.6s.
  ch4_leg2 [character]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 3.0px / 4.6s.
  ch4_leg1 [character]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 3.0px / 4.6s.
  ch4_torso [character]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 3.0px / 4.6s.
  ch4_bisep2 [character]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 3.0px / 4.6s.
  ch4_head [character]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 3.0px / 4.6s.
  ch4_palm2 [character]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 3.0px / 4.6s.
  ch4_palm1 [character]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 3.0px / 4.6s.
  ch4_arm2 [character]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 3.0px / 4.6s.
  ch4_arm1 [character]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 3.0px / 4.6s.
  ch4_bisep1 [character]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 3.0px / 4.6s.
  negative_2 [prop]
      enters at 8:20 (500.0s film time = 17.0s into this scene) on the narration beat "aggressively shouting" (beat 3), row; default idle 2.0px / 5.4s.
  ch3_hand [character]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 3.0px / 4.6s.
  ch3_leg2 [character]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 3.0px / 4.6s.
  ch3_leg1 [character]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 3.0px / 4.6s.
  ch3_pelvis [character]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 3.0px / 4.6s.
  ch3_torso [character]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 3.0px / 4.6s.
  ch3_bisep1 [character]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 3.0px / 4.6s.
  ch3_head [character]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 3.0px / 4.6s.
  ch3_arm1 [character]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 3.0px / 4.6s.
  ch3_nose [character]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 3.0px / 4.6s.
  ch3_eyebrow [character]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 3.0px / 4.6s.
  ch3_eye1 [character]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 3.0px / 4.6s.
  ch3_eye2 [character]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 3.0px / 4.6s.
  ch3_mouth [character]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 3.0px / 4.6s.
  row_divider [numeral]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 1.8px / 6.6s.
  deco_arrow_1 [arrow]
      enters on the fallback recipe (no spec entry — role-based stagger).
  deco_arrow_2 [arrow]
      enters on the fallback recipe (no spec entry — role-based stagger).
  positive_1 [prop]
      enters at 8:12 (492.0s film time = 9.0s into this scene) on the narration beat "calmly propose" (beat 2), row; default idle 2.0px / 5.4s.
  ch_2shadow [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 2.0px / 5.4s.
  ch_2bisep2 [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 2.0px / 5.4s.
  ch_2leg2 [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 2.0px / 5.4s.
  ch_2leg1 [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 2.0px / 5.4s.
  ch_2torso [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 2.0px / 5.4s.
  ch_2arm2 [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 2.0px / 5.4s.
  ch_2_head [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 2.0px / 5.4s.
  ch_2bisep1 [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 2.0px / 5.4s.
  ch_2arm1 [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 2.0px / 5.4s.
  positive_2 [prop]
      enters at 8:12 (492.0s film time = 9.0s into this scene) on the narration beat "calmly propose" (beat 2), row; default idle 2.0px / 5.4s.
  ch_1shadow [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 2.0px / 5.4s.
  ch_1leg1 [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 2.0px / 5.4s.
  ch_1leg2 [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 2.0px / 5.4s.
  ch_1hand1 [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 2.0px / 5.4s.
  ch_1torso [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 2.0px / 5.4s.
  ch_1bisep1 [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 2.0px / 5.4s.
  ch_1arm1 [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 2.0px / 5.4s.
  ch_1_head [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 2.0px / 5.4s.
  ch_1eye1 [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 2.0px / 5.4s.
  ch_1eye2 [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 2.0px / 5.4s.
  ch_1eyebrow1 [card]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 1.6px / 6.4s.
  ch_1eyebrow2 [card]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 1.6px / 6.4s.
  ch_1nose [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 2.0px / 5.4s.
  ch_1lip [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 2.0px / 5.4s.
  result_icon_3 [icon]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 2.2px / 5.0s.
  result_icon_2 [icon]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 2.2px / 5.0s.
  result_icon_1 [icon]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 2.2px / 5.0s.
  guide_grid [decor]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 1.4px / 7.2s.
  text_0 "Wait to debrief"
      enters 0.42s (guider spec, rise-fade); types on over 1.07s (15 chars @ 14/s, 1 line) with a caret.
  text_1 "Shout at crew"
      enters on the fallback recipe (no spec entry — role-based stagger); types on over 0.93s (13 chars @ 14/s, 1 line) with a caret.
  text_2 "Positive (+)"
      enters on the fallback recipe (no spec entry — role-based stagger); types on over 0.86s (12 chars @ 14/s, 1 line) with a caret.
  text_3 "Negative (-)"
      enters on the fallback recipe (no spec entry — role-based stagger); types on over 0.86s (12 chars @ 14/s, 1 line) with a caret.
  text_4 "Rational solving"
      enters on the fallback recipe (no spec entry — role-based stagger); types on over 1.14s (16 chars @ 14/s, 1 line) with a caret.
  text_5 "Aggressive push"
      enters on the fallback recipe (no spec entry — role-based stagger); types on over 1.07s (15 chars @ 14/s, 1 line) with a caret.
  text_6 "Action"
      enters on the fallback recipe (no spec entry — role-based stagger); types on over 0.43s (6 chars @ 14/s, 1 line) with a caret.
  text_7 "Behaviour Type"
      enters on the fallback recipe (no spec entry — role-based stagger); types on over 1.00s (14 chars @ 14/s, 1 line) with a caret.
  text_8 "Influence style"
      enters on the fallback recipe (no spec entry — role-based stagger); types on over 1.07s (15 chars @ 14/s, 1 line) with a caret.

HOLD — once everything has landed, the idle loops, orbit rings, wave drift and
  bubbles keep running. The frame is never completely still.

EXIT — contents slide out to the left over the last 30 frames and fade
  over the back half of that travel, so the frame is clear before the next
  subject arrives. The world does NOT fade — that is what stops a transition
  going blank.

## Notes
- Generated by scripts/build-execution-text.js from sceneRegistry.ts, the frame
  SVG, animation.json, audio-cues.json, text-metrics.json and
  frame-overrides.json. Re-run it after any frame re-export rather than
  hand-editing, or this record drifts from what actually renders.
- To change any element here, edit public/animation/frame-overrides.json
  (see CLAUDE.md "Tweak Any Frame"). Run scripts/inspect-frame.js to see which
  id is which on screen.
- NARRATION TIMING: 8 element(s) in this frame carry data-t authored into
  the artwork by the design project. That timing travels WITH the element, so a
  future rename cannot orphan it the way the mp_v2 redesign orphaned all 133
  entries in audio-cues.json (those are preserved in audio-cues.pre-mp_v2.json).
  To retime one, prefer data-t upstream; frame-overrides.json "at" wins locally.
