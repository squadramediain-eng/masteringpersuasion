FRAME_02 — Six Key Areas — Overview
Appears in the video at: 0:59
Scene length: 900 frames = 30.0 seconds (30fps)
Archetype: Icon grid
Beat: Six tiles reveal in sequence, each with its icon.

WORLD — never enters, never exits
  The canvas, the wave band and the rising bubbles are drawn once by
  src/scenes/WorldLayer.tsx and run continuously behind every scene. This
  frame's own #bg and #waves are stripped by motion.ts (SKIP_IDS) so the
  water never doubles or restarts at a cut.

ENTRANCE — contents slide in from the right, decelerating on the brand curve.

ELEMENTS (22, in the order motion.ts wraps them)

  tile_resolving_disputes [card]
      enters at 1:08 (68.0s film time = 9.0s into this scene) on the narration beat "disputes" (beat 4), stagger; default idle 2.6px / 6.0s.
  tile_new_procedures [card]
      enters at 1:10 (70.0s film time = 11.0s into this scene) on the narration beat "procedures" (beat 5), stagger; default idle 2.6px / 6.0s.
  tile_key_behaviours [card]
      enters at 1:12 (72.0s film time = 13.0s into this scene) on the narration beat "behaviours" (beat 6), stagger; default idle 2.6px / 6.0s.
  tile_correcting_habits [card]
      enters at 1:06 (66.0s film time = 7.0s into this scene) on the narration beat "habits" (beat 3), stagger; default idle 2.6px / 6.0s.
  tile_master_pilot [character]
      enters at 1:04 (64.0s film time = 5.0s into this scene) on the narration beat "master" (beat 2), stagger; idle loop from the guider spec.
  scene_7 [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 4.0px / 5.0s.
  tile_safety_tool [card]
      enters at 1:02 (62.0s film time = 3.0s into this scene) on the narration beat "safety tool" (beat 1), pop; default idle 2.6px / 6.0s.
  bubbles [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 4.0px / 5.0s.
  deco_1 [prop]
      enters on the fallback recipe (no spec entry — role-based stagger); default idle 4.0px / 5.0s.
  env_seaweed [ambient]
      enters on the fallback recipe (no spec entry — role-based stagger).
  text_0 "04"
      enters 0.42s (guider spec, rise-fade); types on over 0.14s (2 chars @ 14/s, 1 line) with a caret.
  text_1 "ResolvingDisputes"
      enters 0.55s (guider spec, rise-fade); types on over 1.21s (17 chars @ 14/s, 2 lines) with a caret.
  text_2 "05"
      enters on the fallback recipe (no spec entry — role-based stagger); types on over 0.14s (2 chars @ 14/s, 1 line) with a caret.
  text_3 "NewProcedures"
      enters on the fallback recipe (no spec entry — role-based stagger); types on over 0.93s (13 chars @ 14/s, 2 lines) with a caret.
  text_4 "06"
      enters on the fallback recipe (no spec entry — role-based stagger); types on over 0.14s (2 chars @ 14/s, 1 line) with a caret.
  text_5 "KeyBehaviours"
      enters on the fallback recipe (no spec entry — role-based stagger); types on over 0.93s (13 chars @ 14/s, 2 lines) with a caret.
  text_6 "03"
      enters on the fallback recipe (no spec entry — role-based stagger); types on over 0.14s (2 chars @ 14/s, 1 line) with a caret.
  text_7 "CorrectingHabits"
      enters on the fallback recipe (no spec entry — role-based stagger); types on over 1.14s (16 chars @ 14/s, 2 lines) with a caret.
  text_8 "02"
      enters on the fallback recipe (no spec entry — role-based stagger); types on over 0.14s (2 chars @ 14/s, 1 line) with a caret.
  text_9 "Master-PilotRel."
      enters on the fallback recipe (no spec entry — role-based stagger); types on over 1.14s (16 chars @ 14/s, 2 lines) with a caret.
  text_10 "01"
      enters on the fallback recipe (no spec entry — role-based stagger); types on over 0.14s (2 chars @ 14/s, 1 line) with a caret.
  text_11 "Safety Tool"
      enters on the fallback recipe (no spec entry — role-based stagger); types on over 0.79s (11 chars @ 14/s, 1 line) with a caret.

HOLD — once everything has landed, the idle loops, orbit rings, wave drift and
  bubbles keep running. The frame is never completely still.

EXIT — contents slide out to the left over the last 30 frames and fade
  over the back half of that travel, so the frame is clear before the next
  subject arrives. The world does NOT fade — that is what stops a transition
  going blank.

## Notes
- Generated by scripts/build-execution-text.js from sceneRegistry.ts, the frame
  SVG, animation.json, audio-cues.json, text-metrics.json and
  frame-overrides.json. Re-run it after any frame re-export rather than
  hand-editing, or this record drifts from what actually renders.
- To change any element here, edit public/animation/frame-overrides.json
  (see CLAUDE.md "Tweak Any Frame"). Run scripts/inspect-frame.js to see which
  id is which on screen.
- NARRATION TIMING: 6 element(s) in this frame carry data-t authored into
  the artwork by the design project. That timing travels WITH the element, so a
  future rename cannot orphan it the way the mp_v2 redesign orphaned all 133
  entries in audio-cues.json (those are preserved in audio-cues.pre-mp_v2.json).
  To retime one, prefer data-t upstream; frame-overrides.json "at" wins locally.
